services:
  postgres-db:
    image: postgres:16
    container_name: langlearn-db
    environment:
      POSTGRES_DB: langlearn_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: